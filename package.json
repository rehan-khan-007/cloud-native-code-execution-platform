{
  "name": "cloud-native-code-execution-platform",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["apps/*", "services/*", "packages/*"],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "jest",
    "lint": "eslint ."
  }
}
